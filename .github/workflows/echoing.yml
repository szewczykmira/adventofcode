name: Echoing

# TODO: search proper options
on: [pull_request]

jobs:
  build:
    name: Wow!

    runs-on: ubuntu-latest
    steps: 
      # Check-out repository
      - uses: actions/checkout@v2

      - name: greet
        run: echo "Hello my friend"
